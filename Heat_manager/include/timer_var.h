#include <includes.h>
extern timer_t timer3;
/**
 * void clock() - The function use to run the digital clock.
 *
 * Function use to calculate the hours, minutes, seconds by using timer API.
 *
 */
void clock(void *);
