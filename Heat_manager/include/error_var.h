#include "includes.h"
#include <error.h>

/**
 *
 * error_callback() - The function use to display error message.
 * 
 * Function will display error message.
 * when i2c device not connected with hardware or no ACK came from i2c device.
 *
 * 
 */
void error_callback(uint8_t);
